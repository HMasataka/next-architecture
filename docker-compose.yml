version: '3.9'

services:
  api:
    image: openapitools/openapi-generator-cli:v6.6.0
    volumes:
      - ./oapi:/opt/schema
      - ./src/autogenerated:/opt/output
    command: ["generate", "-i", "/opt/schema/openapi.yaml", "-g", "typescript-fetch", "-o", "/opt/output"]
